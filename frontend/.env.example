# GenLayer Studionet (default) atau testnetAsimov
NEXT_PUBLIC_NETWORK=studionet

# Contract address setelah deploy di studio.genlayer.com
NEXT_PUBLIC_CONTRACT_ADDRESS=0xYourContractAddressHere

# Pinata IPFS (opsional) - pinata.cloud -> API Keys -> Files:Write -> JWT
NEXT_PUBLIC_PINATA_JWT=
